# 接口安全快速测试记录模板

## 1. 未登录访问后台页面

### 1.1 测试编号：A-01

**测试项目：** 未登录访问后台首页

**测试步骤：** 无痕浏览，访问 `/user/dashboard`

**测试类型：** 黑盒测试

**测试目标：**

验证未登录情况下访问，是否能够正跳转登录页。

**测试条件：**

1. 请求中未携带 `Authorization` 请求头。
2. 清空浏览器 `localStorage`

**预期结果：** 跳转登录页。

**实际结果：** 跳转登录页。

**测试结论：** 通过

---

### 1.2 测试编号：A-02

**测试项目：** 未登录访问知识管理页

**测试步骤：** 无痕浏览，访问 `/user/knowledge`

**测试类型：** 黑盒测试

**测试目标：**

验证未登录情况下访问，是否能够正跳转登录页。

**测试条件：**

1. 请求中未携带 `Authorization` 请求头。
2. 清空浏览器 `localStorage`

**预期结果：** 跳转登录页。

**实际结果：** 跳转登录页。

**测试结论：** 通过

---

### 1.3 测试编号：A-03

**测试项目：** 未登录访问咨询记录页

**测试步骤：** 无痕浏览，访问 `/user/consultations`

**测试类型：** 黑盒测试

**测试目标：**

验证未登录情况下访问，是否能够正跳转登录页。

**测试条件：**

1. 请求中未携带 `Authorization` 请求头。
2. 清空浏览器 `localStorage`

**预期结果：** 跳转登录页。

**实际结果：** 跳转登录页。

**测试结论：** 通过

---

### 1.4 测试编号：A-04

**测试项目：** 未登录访问情绪日志页

**测试步骤：** 无痕浏览，访问 `/user/emotion`

**测试类型：** 黑盒测试

**测试目标：**

验证未登录情况下访问，是否能够正跳转登录页。

**测试条件：**

1. 请求中未携带 `Authorization` 请求头。
2. 清空浏览器 `localStorage`

**预期结果：** 跳转登录页。

**实际结果：** 跳转登录页。

**测试结论：** 通过

---

## 2. 未登录访问管理端 API 测试

### 2.1 测试编号：B-01

**测试接口：** `/api/knowledge/article`

**请求方法：** POST

**测试类型：** 黑盒测试

**测试目标：**

验证知识文章新增接口在未登录情况下，是否能够正确拒绝访问。

**测试条件：**

1. 请求中未携带 `Authorization` 请求头。
2. 请求体携带完整文章 JSON 数据。
3. 使用 Postman 直接调用后端 API。

**预期结果：** 接口应拒绝未授权访问，返回 401 未登录或 403 无权限，不应新增知识文章数据。

**实际结果：** 未携带 `Authorization` 请求头，接口仍返回 `code=200`，并成功新增文章。

**测试结论：** 不通过

**风险等级：** 高

---

### 2.2 测试编号：B-02

**测试接口：** `/api/knowledge/article/{id}/status`

**请求方法：** PUT

**测试类型：** 黑盒测试

**测试目标：**

验证知识文章发布/下线接口在未登录情况下，是否能够正确拒绝访问。

**测试条件：**

1. 请求中未携带 `Authorization` 请求头。
2. 请求路径中使用真实存在的文章 ID。
3. 请求参数或请求体携带完整状态更新数据。
4. 使用 Postman 直接调用后端 API。

**预期结果：** 接口应拒绝未授权访问，返回 401 未登录或 403 无权限，不应修改文章状态。

**实际结果：**  未携带 `Authorization` 请求头，接口仍返回 `code=200`，并成功更新状态 `status`。

**测试结论：** 不通过

**风险等级：** 高

---

### 2.3 测试编号：B-03

**测试接口：** `/api/knowledge/article/{id}`

**请求方法：** DELETE

**测试类型：** 黑盒测试

**测试目标：**

验证知识文章删除接口在未登录情况下，是否能够正确拒绝访问。

**测试条件：**

1. 请求中未携带 `Authorization` 请求头。
2. 请求路径中使用真实存在的文章 ID。
3. 使用 Postman 直接调用后端 API。

**预期结果：** 接口应拒绝未授权访问，返回 401 未登录或 403 无权限，不应删除知识文章数据。

**实际结果：** 未携带 `Authorization` 请求头，接口仍返回 `code=200`，并成功删除指定文章。

**测试结论：** 不通过

**风险等级：** 高

---

### 2.4 测试编号：B-04

**测试接口：** `/api/psychological-chat/sessions`

**请求方法：** GET

**测试类型：** 黑盒测试

**测试目标：**

验证咨询会话分页接口在未登录情况下，是否能够正确拒绝访问。

**测试条件：**

1. 请求中未携带 `Authorization` 请求头。
2. 请求参数携带正常分页参数，例如 `page=1`、`size=10`。
3. 使用 Postman 直接调用后端 API。

**预期结果：** 接口应拒绝未授权访问，返回 401 未登录或 403 无权限，不应返回咨询会话数据。

**实际结果：** 未携带 `Authorization` 请求头，接口仍返回 `code=200`，并成功返回会话列表。

**测试结论：** 不通过

**风险等级：** 高

---

### 2.5 测试编号：B-05

**测试接口：** `/api/psychological-chat/sessions/{sessionId}/messages`

**请求方法：** GET

**测试类型：** 黑盒测试

**测试目标：**

验证咨询消息详情接口在未登录情况下，是否能够正确拒绝访问。

**测试条件：**

1. 请求中未携带 `Authorization` 请求头。
2. 请求路径中使用真实存在的会话 ID。
3. 使用 Postman 直接调用后端 API。

**预期结果：** 接口应拒绝未授权访问，返回 401 未登录或 403 无权限，不应返回聊天消息内容。

**实际结果：** 未携带 `Authorization` 请求头，接口仍返回 `code=200`，并成功返回指定ID的详情信息。

**测试结论：** 不通过

**风险等级：** 高

---

### 2.6 测试编号：B-06

**测试接口：** `/api/emotion-diary/admin/page`

**请求方法：** GET

**测试类型：** 黑盒测试

**测试目标：**

验证后台情绪日志分页接口在未登录情况下，是否能够正确拒绝访问。

**测试条件：**

1. 请求中未携带 `Authorization` 请求头。
2. 请求参数携带正常分页参数，例如 `page=1`、`size=10`。
3. 使用 Postman 直接调用后端 API。

**预期结果：** 接口应拒绝未授权访问，返回 401 未登录或 403 无权限，不应返回后台情绪日志数据。

**实际结果：** 未携带 `Authorization` 请求头，接口仍返回 `code=200`，并成功返回情绪日志全部信息。

**测试结论：** 不通过

**风险等级：** 高

---

### 2.7 测试编号：B-07

**测试接口：** `/api/emotion-diary/admin/{id}`

**请求方法：** DELETE

**测试类型：** 黑盒测试

**测试目标：**

验证后台情绪日志删除接口在未登录情况下，是否能够正确拒绝访问。

**测试条件：**

1. 请求中未携带 `Authorization` 请求头。
2. 请求路径中使用真实存在的情绪日志 ID。
3. 使用 Postman 直接调用后端 API。

**预期结果：** 接口应拒绝未授权访问，返回 401 未登录或 403 无权限，不应删除情绪日志数据。

**实际结果：** 未携带 `Authorization` 请求头，接口仍返回 `code=200`，并成功删除对应ID记录。

**测试结论：** 不通过

**风险等级：** 高

---

### 2.8 测试编号：B-08

**测试接口：** `/api/file/upload`

**请求方法：** POST

**测试类型：** 黑盒测试

**测试目标：**

验证文件上传接口在未登录情况下，是否能够正确拒绝访问或限制业务类型。

**测试条件：**

1. 请求中未携带 `Authorization` 请求头。
2. 请求体使用 `multipart/form-data`，携带真实图片文件。
3. 请求参数包含业务类型，例如 `businessType=avatar` 或 `businessType=knowledge_article`。
4. 使用 Postman 直接调用后端 API。

**预期结果：** 接口应拒绝未授权上传，返回 401 未登录或 403 无权限，或仅允许明确公开的上传类型。

**实际结果：** 未携带 `Authorization` 请求头，接口仍返回 `code=200`，并成功上传图片。

**测试结论：** 不通过

**风险等级：** 高

---

### 2.8 测试编号：B-09

**测试接口：** `/api/knowledge/article/page`

**请求方法：** GET

**测试类型：** 黑盒测试

**测试目标：**

验证未登录情况下，是否能够获取后台知识库文章。

**测试条件：**

1. 请求中未携带 `Authorization` 请求头。
2. 使用 Postman 直接调用后端 API。

**预期结果：** 接口应拒绝请求，返回 401 未登录或 403 无权限。

**实际结果：** 未携带 `Authorization` 请求头，接口仍返回 `code=200`，并成功显示文章。

**测试结论：** 不通过

**风险等级：** 中

---

## 3. 普通用户越权访问管理端 API 测试

### 3.1 测试编号：G-01

**测试接口：** `/api/knowledge/article`

**请求方法：** POST

**测试类型：** 灰盒测试

**测试目标：**

验证普通用户携带合法 Token 时，是否能够越权调用知识文章新增接口。

**测试条件：**

1. 使用普通用户账号登录系统。
2. 从浏览器 localStorage 或 Network 中复制普通用户 Token。
3. 请求头携带 `Authorization: Bearer 普通用户Token`。
4. 请求体携带完整文章 JSON 数据。
5. 使用 Postman 直接调用后端 API。

**预期结果：** 接口应拒绝普通用户访问，返回 403 无权限，不应新增知识文章数据。

**实际结果：** 普通用户携带合法 Token 调用时，接口仍返回 `code=200`，并成功更新文章。

**测试结论：** 不通过

**风险等级：** 高

---

### 3.2 测试编号：G-02

**测试接口：** `/api/knowledge/article/{id}`

**请求方法：** DELETE

**测试类型：** 灰盒测试

**测试目标：**

验证普通用户携带合法 Token 调用时，是否能够越权删除知识文章。

**测试条件：**

1. 使用普通用户账号登录系统。
2. 请求头携带普通用户 Token。
3. 请求路径中使用真实存在的文章 ID。
4. 使用 Postman 直接调用后端 API。
5. 测试后通过数据库检查文章 `deleted` 字段是否变化。

**预期结果：** 接口应拒绝普通用户删除文章，返回 403 无权限，数据库中文章状态不应改变。

**实际结果：** 普通用户携带合法 Token 调用时，接口仍返回 `code=200`，并成功删除指定文章。

**测试结论：** 不通过

**风险等级：** 高

---

### 3.3 测试编号：G-03

**测试接口：** `/api/emotion-diary/admin/page`

**请求方法：** GET

**测试类型：** 灰盒测试

**测试目标：**

验证普通用户携带合法 Token 时，是否能够越权访问后台情绪日志分页数据。

**测试条件：**

1. 使用普通用户账号登录系统。
2. 请求头携带普通用户 Token。
3. 请求参数携带正常分页参数。
4. 使用 Postman 直接调用后端 API。

**预期结果：** 接口应拒绝普通用户访问后台情绪日志，返回 403 无权限，或仅返回当前用户自己的日志数据。

**实际结果：** 普通用户携带合法 Token 调用时，接口仍返回 `code=200`，并成功显示数据。

**测试结论：** 通过

**风险等级：** 高

---

### 3.4 测试编号：G-04

**测试接口：** `/api/psychological-chat/sessions`

**请求方法：** GET

**测试类型：** 灰盒测试

**测试目标：**

验证普通用户携带合法 Token 时，是否能够越权访问后台咨询会话列表。

**测试条件：**

1. 使用普通用户账号登录系统。
2. 请求头携带普通用户 Token。
3. 请求参数携带正常分页参数。
4. 使用 Postman 直接调用后端 API。

**预期结果：** 接口应拒绝普通用户访问后台咨询记录，返回 403 无权限，或仅返回当前用户自己的咨询会话。

**实际结果：** 普通用户携带合法 Token 调用时，接口仍返回 `code=200`，并成功显示数据。

**测试结论：** 不通过

**风险等级：** 高

---

## 4. Token 异常测试

### 4.1 测试编号：T-01

**测试接口：** `/api/knowledge/article`

**请求方法：** POST

**测试类型：** 黑盒测试

**测试目标：**

验证接口在携带无效 Token 时，是否能够正确拒绝访问。

**测试条件：**

1. 请求头携带 `Authorization: Bearer abc123.invalid.token`。
2. 请求体携带完整文章 JSON 数据。
3. 使用 Postman 直接调用后端 API。

**预期结果：** 接口应返回 401 Token 无效或 403 无权限，不应新增知识文章数据。

**实际结果：** 携带无效 Token 时，接口仍返回 `code=200`，并成功更新数据。

**测试结论：** 不通过

**风险等级：** 高

---

### 4.2 测试编号：T-02

**测试接口：** `/api/emotion-diary/admin/page`

**请求方法：** GET

**测试类型：** 黑盒测试

**测试目标：**

验证接口在携带伪造 Token 时，是否能够正确拒绝访问。

**测试条件：**

1. 请求头携带随机或篡改后的 Token。
2. 请求参数携带正常分页参数。
3. 使用 Postman 直接调用后端 API。

**预期结果：** 接口应返回 401 Token 无效或 403 无权限，不应返回后台情绪日志数据。

**实际结果：** 携带伪造 Token 时，接口仍返回 `code=200`，并成功更新数据。

**测试结论：** 不通过

**风险等级：** 高

